extends layout

block scripts
  script(src='../scripts/user.js')

block content
  .w3-card-4
    h1#name #{user.displayName}
    h3#lon
    h3#lat
    script.
      userName="#{user.displayName}";
      userId="#{user.user_id}";
    #map
