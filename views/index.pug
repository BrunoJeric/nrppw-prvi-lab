extends layout

block content
  .w3-container
    if locals.user
      h4 Go to user Map to see your location!
    else
      h4 You are not logged in! Please #[a(href="/login") Log In] to see your location.
